name: CI — package validation

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  validate:
    name: Structure + manifest + retrieval validation
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Lint manifest.json
        run: |
          python3 - <<'PY'
          import json, os
          m = json.load(open('manifest.json'))
          assert m['name'] == 'shortdrama-producer', 'name mismatch'
          assert m['version'], 'version missing'
          assert m['entrypoints'].get('AGENTS.md') and m['entrypoints'].get('SKILL.md'), 'entrypoints missing'
          # every declared file must exist
          for k in ('scripts', 'references'):
              for rel in m['dependencies'].get(k, []):
                  assert os.path.exists(rel), f'missing dependency file: {rel}'
          print(f"manifest OK (v{m['version']}, {m['data']['docs']} docs, {m['data']['character_cards']} cards)")
          PY

      - name: Lint AGENTS.md / SKILL.md structure
        run: |
          python3 - <<'PY'
          import re
          # SKILL.md: must be YAML frontmatter with name + description (agentskills.io spec)
          txt = open('SKILL.md').read()
          assert txt.startswith('---'), 'SKILL.md must start with YAML frontmatter'
          m = re.match(r'^---\n(.*?)\n---\n', txt, re.S)
          assert m, 'SKILL.md frontmatter parse failed'
          assert 'name:' in m.group(1) and 'description:' in m.group(1), 'SKILL.md needs name+description'
          print('SKILL.md frontmatter OK')
          # AGENTS.md: 2026 cross-tool standard — markdown with title + role + workflow sections
          txt = open('AGENTS.md').read()
          assert 'shortdrama-producer' in txt, 'AGENTS.md missing agent name'
          for sec in ('## Role', '## Triggers', '## Workflow', '## Boundaries'):
              assert sec in txt, f'AGENTS.md missing section {sec}'
          print('AGENTS.md structure OK')
          PY

      - name: Retrieval smoke test
        run: |
          python3 scripts/seedance_search.py "police helicopter chasing a white Escalade at night" 3 | python3 -c "
          import sys, json
          d = json.load(sys.stdin)
          hits = d.get('hits', [])
          assert len(hits) >= 1, 'no hits returned'
          for h in hits:
              assert h.get('prompt_clean') and len(h['prompt_clean']) > 200, 'prompt_clean too short or missing'
              assert 'structure' in h, 'structure missing'
          print(f'smoke OK: {len(hits)} hits, top score={hits[0][\"score\"]}')
          "

      - name: Structure anti-rot validation
        run: python3 scripts/validate_structure.py

      - name: Diagnoser smoke test
        run: |
          printf 'A girl in the rain. Add background music. Diegetic sound only, no music.\n' \
            | python3 scripts/diagnose_prompt.py --json > /tmp/diag.json
          python3 - <<'PY2'
          import json
          d = json.load(open('/tmp/diag.json'))
          assert d['contradictions'], 'diagnoser failed to catch the music contradiction'
          assert d['missing_sections'], 'diagnoser reported no missing sections for a bare prompt'
          assert any('300' in w for w in d['warnings']), 'diagnoser missed the too-short warning'
          print('diagnoser smoke OK:', len(d['contradictions']), 'contradiction(s),',
                len(d['missing_sections']), 'missing section(s)')
          PY2

      - name: install.sh syntax check
        run: bash -n install.sh && echo "install.sh syntax OK"

      - name: README bilingual completeness
        run: |
          python3 - <<'PY'
          txt = open('README.md').read()
          assert '## English' in txt, 'README missing English section'
          assert '## 中文' in txt, 'README missing Chinese section'
          print('README bilingual OK')
          PY
